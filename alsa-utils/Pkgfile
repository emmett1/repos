# Depends on: alsa-lib 

name=alsa-utils
version=1.2.10
release=1
source=(https://www.alsa-project.org/files/pub/utils/$name-$version.tar.bz2
	alsa)
build_opt="--disable-bat --disable-xmlto"

post_build() {
	install -Dm755 $SRC/alsa $PKG/etc/rc.d/alsa
}
